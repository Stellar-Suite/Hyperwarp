# New Protocol
See `stellar_protocol`

## Hyperwarp Behavior
When Hyperwarp is injected into a process, it will export a unix socket in `/dev/shm/` to allow the streamer process to coordinate with it.